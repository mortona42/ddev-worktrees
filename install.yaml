name: ddev-worktree

project_files:
  - commands/host/worktree
  - commands/host/worktree-remove

ddev_version_constraint: '>= v1.24.0'

pre_install_actions: []

post_install_actions:
  - chmod +x commands/host/worktree
  - chmod +x commands/host/worktree-remove

yaml_read_files: {}
